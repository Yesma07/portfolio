---

---

<section id="contact">
  <div class="container">
    <h2 class="section-title">Abrir Canal de Comunicación</h2>
    <p class="contact-message">
      ¿Tienes una idea, un proyecto o una oportunidad? Me encantaría escucharla.
      Usa la terminal de abajo para enviar tu transmisión.
    </p>

    <div class="terminal-window">
      <div class="terminal-header">
        <span class="pulse" style="--delay: 0s;">.</span>
        <span class="pulse" style="--delay: 0.2s;">.</span>
        <span class="pulse" style="--delay: 0.4s;">.</span>
        <span class="header-text">[ INCOMING TRANSMISSION ]</span>
        <span class="pulse" style="--delay: 0.4s;">.</span>
        <span class="pulse" style="--delay: 0.2s;">.</span>
        <span class="pulse" style="--delay: 0s;">.</span>
      </div>
      <form
        id="contact-form"
        action="https://formspree.io/f/mgvpkleq"
        method="POST"
        class="contact-form"
        autocomplete="off"
      >
        <div class="form-group">
          <label for="name">> Nombre_de_usuario:</label>
          <input type="text" id="name" name="name" required />
        </div>
        <div class="form-group">
          <label for="email">> Frecuencia_de_email:</label>
          <input type="email" id="email" name="email" required />
        </div>
        <div class="form-group">
          <label for="message">> Mensaje:</label>
          <textarea id="message" name="message" rows="4" required></textarea>
        </div>
        <button type="submit" class="contact-button">Enviar Transmisión</button>
      </form>
    </div>
  </div>
</section>

<style>
  #contact {
    padding: 4rem 2rem 6rem;
    background: linear-gradient(rgba(1, 0, 20, 0.8), rgba(1, 0, 20, 0.2));
  }
  .container {
    max-width: 800px;
    margin: 0 auto;
  }
  .section-title {
    font-size: 2rem;
    text-align: center;
    margin-bottom: 1.5rem;
  }
  .contact-message {
    font-size: 1.2rem;
    line-height: 1.7;
    margin-bottom: 3rem;
    color: var(--color-text);
    text-align: center;
  }
  .terminal-window {
    border: 2px solid var(--color-accent-cyan);
    background-color: rgba(1, 0, 20, 0.5);
    padding: 1.5rem;
  }
  .terminal-header {
    color: var(--color-accent-cyan);
    font-family: var(--font-family-body);
    margin-bottom: 2rem;
    text-align: center;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 0.5rem;
  }
  .pulse {
    color: var(--color-accent-magenta);
    animation: pulse-animation 1.5s infinite;
    animation-delay: var(--delay);
  }
  @keyframes pulse-animation {
    0%,
    100% {
      opacity: 0.2;
      transform: scale(0.8);
    }
    50% {
      opacity: 1;
      transform: scale(1.2);
    }
  }
  .contact-form {
    text-align: left;
  }
  .form-group {
    margin-bottom: 1.5rem;
  }
  .form-group label {
    display: block;
    margin-bottom: 0.5rem;
    color: var(--color-accent-cyan);
  }
  .form-group input,
  .form-group textarea {
    width: 100%;
    padding: 0.75rem;
    background-color: transparent;
    border: 2px solid var(--color-accent-cyan);
    color: var(--color-text);
    font-family: var(--font-family-body);
    font-size: 1rem;
    box-sizing: border-box;
  }
  .form-group input:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--color-accent-magenta);
    box-shadow: 0 0 10px var(--color-accent-magenta);
    animation: blink-caret 0.75s step-end infinite;
  }
  @keyframes blink-caret {
    from,
    to {
      border-right: 2px solid var(--color-accent-magenta);
    }
    50% {
      border-right-color: transparent;
    }
  }
  .contact-button {
    display: block;
    margin: 2rem auto 0;
    font-family: var(--font-family-heading);
    font-size: 1.2rem;
    color: var(--color-accent-yellow);
    background-color: transparent;
    border: 2px solid var(--color-accent-yellow);
    padding: 1rem 2rem;
    text-decoration: none;
    cursor: pointer;
    transition: all 0.3s;
  }
  .contact-button:hover {
    background-color: var(--color-accent-yellow);
    color: var(--color-bg);
    box-shadow: 0 0 20px var(--color-accent-yellow);
  }

  @media (max-width: 480px) {
    .terminal-header {
      gap: 0.25rem;
      flex-wrap: nowrap;
    }
    .header-text {
      font-size: 0.9em;
    }
  }
</style>
